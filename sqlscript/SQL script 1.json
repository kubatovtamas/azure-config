{
	"name": "SQL script 1",
	"properties": {
		"content": {
			"query": "-- SELECT county, COUNT(policyID) AS count_policyID, MAX(eq_site_limit) AS max_eq_site_limit\n-- FROM (\n--   SELECT *, ROW_NUMBER() OVER (PARTITION BY county ORDER BY eq_site_limit DESC) AS row_num\n--   FROM [insurance].[FL_insurance_sample]\n-- ) t\n-- WHERE row_num = 1\n-- GROUP BY county\n\n\n-- SELECT top 1000 *, ROW_NUMBER() OVER (PARTITION BY county ORDER BY eq_site_limit DESC) AS row_num\n-- FROM [insurance].[FL_insurance_sample]\n\n-- where policyID = 949187\n\nselect count(*)\nfrom [insurance].[FL_insurance_sample]\nunion all\nselect DISTINCT count(policyID)\nfrom [insurance].[FL_insurance_sample];",
			"metadata": {
				"language": "sql"
			},
			"currentConnection": {
				"databaseName": "DedicatedPool",
				"poolName": "DedicatedPool"
			},
			"resultLimit": 5000
		},
		"type": "SqlQuery"
	}
}